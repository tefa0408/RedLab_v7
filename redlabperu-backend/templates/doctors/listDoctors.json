{
  "status": true,
  "total": "{{data.total}}",
  "count": "{{data.count}}",
  "data": {
    "{{#each data.rows}}": {
      "doctor": {
        "id": "{{#? this.Doctor.id}}",
        "doctorName": "{{#? this.Doctor.doctorName}}" 
      }
    }
  }
}